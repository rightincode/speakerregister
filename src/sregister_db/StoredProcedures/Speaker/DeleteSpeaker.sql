CREATE PROCEDURE [dbo].[DeleteSpeaker]
	@Id		INT
AS
BEGIN TRY
	BEGIN TRANSACTION

		DELETE 
		FROM dbo.Speaker
		WHERE Id = @Id

	COMMIT TRANSACTION
END TRY
BEGIN CATCH
	IF @@TRANCOUNT > 0
		ROLLBACK TRANSACTION;

	THROW;
END CATCH

CREATE PROCEDURE [dbo].[DeleteSpeaker]
	@Id		INT
AS
BEGIN TRY
	BEGIN TRANSACTION

		UPDATE dbo.Speaker
		SET [Deleted] = 1
		WHERE Id = @Id

	COMMIT TRANSACTION
END TRY
BEGIN CATCH
	IF @@TRANCOUNT > 0
		ROLLBACK TRANSACTION;

	THROW;
END CATCH
